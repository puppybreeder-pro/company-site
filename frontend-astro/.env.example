# Web3Forms access key — get yours at https://web3forms.com
PUBLIC_WEB3FORMS_ACCESS_KEY=your-access-key-here

# Google Analytics 4 — optional. Measurement ID from GA Admin → Data streams (format G-XXXXXXXXXX).
# Loaded via Partytown (web worker) so gtag does not block the main thread.
PUBLIC_GA_MEASUREMENT_ID=